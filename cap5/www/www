var app = require('../app');

// Inicia o servidor
var server = app.listen(3000, function () {
  var host = server.address().address;
  var port = server.address().port;
  console.log('estamos funfando no endereco http://%s:$s', host, port);
});
